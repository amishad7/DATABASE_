import 'package:get/get.dart';

class ThemeController extends GetxController {
  // ThemeMode themeMode = ThemeMode(isDark: false);

  // toDarkMode() {
  //   themeMode.isDark = !themeMode.isDark;
  // }
}
